# Core Python dependencies for the full ORFS -> MacroPlacement -> RL pipeline.
# This file is intentionally kept to the minimal set used by the current
# pipeline scripts that remain in the repository.

absl-py
numpy<2
matplotlib
sortedcontainers
torch>=2.1,<2.5

# Convenience dependency kept for CSV/JSON result inspection in ad-hoc analysis.
pandas
